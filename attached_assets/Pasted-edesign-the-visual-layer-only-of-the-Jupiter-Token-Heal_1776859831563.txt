edesign the visual layer only of the "Jupiter Token Health Dashboard" — a real-time Solana token intelligence web app. Do NOT change any functionality, API calls, data logic, backend, search behavior, wallet parsing, token data, or component structure. Only upgrade the visual presentation layer.
AESTHETIC DIRECTION — "Solana War Room": Think Bloomberg Terminal meets Cyberpunk 2077 meets a $500M hedge fund trading floor. Dark, precise, alive, and expensive. Every pixel should feel like it costs money.
TYPOGRAPHY: Use "Orbitron" for the hero title TOKEN HEALTH DASHBOARD with letter-spacing 0.15em and a chromatic aberration CSS effect (RGB split red +2px, blue -2px) on hover plus a scanline pseudo-element overlay at 3% opacity. Use "Rajdhani" weight 600 for all subheadings and card titles. Use "JetBrains Mono" for all prices, wallet addresses, token symbols, and numeric data. Use "Syne" weight 800 for the tagline "Know what's real. Trade smarter." Hero title gets an animated gradient text: linear-gradient(135deg, #00FFA3, #00D4FF, #7B2FFF) with background-size 200% and slow shifting animation. Each word of the hero title staggers in on load sliding up from 40px with opacity 0 to 1, delays 0ms, 150ms, 300ms, using cubic-bezier(0.16, 1, 0.3, 1).
BACKGROUND SYSTEM — 5 layered: Base color #040B14. Layer 2 is an animated gradient mesh with 3 slow-drifting radial gradients: Solana green #00FFA3 at 8% opacity top-left, Jupiter purple #7B2FFF at 10% opacity bottom-right, electric cyan #00D4FF at 6% opacity center-right, animating background-position over 20s ease infinite alternate. Layer 3 is a CSS perspective grid floor with converging lines toward horizon in #00FFA3 at 4% opacity. Layer 4 is an SVG feTurbulence grain noise overlay at 4% opacity for texture. Layer 5 is a radial vignette gradient black at edges transparent at center. Add 60 floating particles (mix of 1px dots and 4px hexagons) in colors #00FFA3, #7B2FFF, #FFD700 at 40-60% opacity with slow upward drift and some pulsing scale 1 to 1.4 to 1 over 3s. Also place a large faint 3D Solana SVG logo behind the hero rotating on Y-axis 360deg over 30s at 3% opacity for depth.
TICKER BAR: Background rgba(0,255,163,0.05) with 1px bottom border rgba(0,255,163,0.2). Each token wrapped in a pill chip: background #0D1F2D, border 1px solid rgba(0,255,163,0.2), border-radius 20px, padding 4px 12px. Price increases in #00FFA3, decreases in #FF3B6F. Numbers briefly scale to 1.1 on update. Separators become ◆ in #7B2FFF. Ticker pauses smoothly on hover. Speed 40s loop.
NAVBAR: backdrop-filter blur(20px) saturate(180%), background rgba(4,11,20,0.85), border-bottom 1px solid rgba(0,255,163,0.1). Logo icon gets a subtle rotating hue-rotate glow 0deg to 360deg over 8s infinite. Brand name splits in weight — "JUPITER" lighter, "TOKEN HEALTH" bold, slides in from left on load. LIVE dot gets a radar ping animation — two rings expanding from center scaling 1 to 2.5 while fading opacity 0.8 to 0 over 1.5s infinite. "Powered by Jupiter" button becomes glassmorphism: background rgba(123,47,255,0.15), border 1px solid rgba(123,47,255,0.4), shimmer sweep on hover.
HERO SEARCH BAR: background rgba(13,31,45,0.8), border 1px solid rgba(0,255,163,0.2), border-radius 16px, backdrop-filter blur(10px). On focus: border rgba(0,255,163,0.7), box-shadow 0 0 0 3px rgba(0,255,163,0.1) and 0 0 30px rgba(0,255,163,0.15), background deepens. Search icon animates to spinner while typing. Placeholder cycles with typewriter effect through "Search $SOL...", "Paste wallet address...", "Try $BONK...", "Search token mint..." — each typing out then deleting. Recent token pills become frosted glass: background rgba(255,255,255,0.04), border rgba(255,255,255,0.08), on hover border becomes #00FFA3 at 50% opacity and transform translateY(-2px), staggered entrance 50ms per chip.
FEATURE CARDS (Jupiter API, TradingView, On-Chain Data): background rgba(13,31,45,0.6), border 1px solid rgba(255,255,255,0.06), border-radius 20px, backdrop-filter blur(16px). Each card has a 2px top accent line via ::before — Card 1 gradient #7B2FFF to #00FFA3, Card 2 gradient #00FFA3 to #00D4FF, Card 3 gradient #FF3B6F to #FF8C00. Each card has a corner glow via ::after — 120px radial gradient in matching color at 8% opacity bleeding from top-right corner. Icons wrapped in 48px circle with matching gradient background at 15% opacity and matching border at 30% opacity. On hover: translateY(-6px), border brightens, box-shadow 0 20px 60px rgba(0,0,0,0.5) plus colored glow, spring easing cubic-bezier(0.34, 1.56, 0.64, 1). Cards stagger in on scroll via Intersection Observer: translateY(50px) opacity 0 to final state, delays 0ms, 150ms, 300ms, duration 600ms.
TAGLINE SECTION: "Know what's real." in Syne 800 64px white, words split and stagger in from bottom on scroll. "Trade smarter." in Syne 800 64px with animated gradient #00FFA3 to #7B2FFF to #FF3B6F cycling 3s, plus an underline that draws itself left to right using clip-path animation. A horizontal light beam — 60px tall, 100% wide, color #00FFA3 at 20% opacity, blurred filter blur 20px — drifts slowly above the text in a loop.
PILL BUTTONS: FREE button gets background linear-gradient(135deg, #00FFA3, #00C87A), black text, box-shadow 0 0 20px rgba(0,255,163,0.4), scales 1.05 on hover with intensified glow. NO LOGIN button: transparent with purple border rgba(123,47,255,0.6), color #A78BFF, fills rgba(123,47,255,0.15) on hover. REAL-TIME button: transparent with red border rgba(255,59,111,0.6), color #FF3B6F, prepend blinking ● dot at 1s blink interval. All pills: border-radius 100px, Rajdhani 700, letter-spacing 0.1em, spring transition cubic-bezier(0.34, 1.56, 0.64, 1).
DONATE BUTTON: background linear-gradient(135deg, #7B2FFF, #FF3B6F), border-radius 100px, breathing glow animation oscillating box-shadow between rgba(123,47,255,0.5) and rgba(255,59,111,0.6) over 2s infinite alternate. Diamond icon rotates 360deg on hover 400ms ease. Scales 1.08 on hover.
SCROLL & PARALLAX: Smooth scroll on html. Navbar padding shrinks after 80px scroll, 300ms transition. Hero background mesh moves at 0.3x scroll speed, particles at 0.5x via JS scroll listener using transform translateY. 2px scroll progress bar at very top of viewport fills left to right in #00FFA3 as user scrolls.
MICRO-INTERACTIONS: Custom crosshair cursor in #00FFA3 with trailing dot, all clickables use custom cursor. Token logos scale 1.1 with hue-rotate on hover. Price numbers flash #FFD700 on data update then return. All interactive elements have 200-300ms transitions. Page fades in 300ms on load. Failed search triggers red glow #FF3B6F with CSS shake keyframe. Loading states use shimmer skeleton screens not spinners.
POLISH RULES: Never use pure #000000 — use #040B14 minimum. Never use pure #FFFFFF for text — use #E2E8F0. All borders semi-transparent never solid. All shadows use nearest accent color not black. Strict 8px spacing grid throughout. All animations use will-change transform for GPU acceleration. All entrance animations use transform never height or margin to prevent layout shifts. Backdrop-filter blur on every overlay and floating element. Font size scale locked to 12, 14, 16, 20, 24, 32, 48, 64, 80px only. Line heights: body 1.6, headings 1.1, mono data 1.4.